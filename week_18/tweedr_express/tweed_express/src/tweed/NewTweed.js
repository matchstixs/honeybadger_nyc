import React, {Component} from 'react'

class NewTweed extends Component {
    constructor(props) {
        super(props)
        this.state= {
            
        }
    }

    render() {
        return (

            - Should render a form for a new tweed
            - Should clear itself after a new tweed is rendered
        )};
};
