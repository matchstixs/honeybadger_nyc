class bank {
  constructor() {
  bank.name = name,
  // - a name -> `name`
  bank.address = address,
  // - an address -> `address`
  bank.accounts = [
    let checking = Math.random(),
    let savings = Math.random()],
  // - accounts -> `accounts`
  // 	- a bank starts with a checking AND savings account

  information = function() {
  // - display its information -> `information`
    return bank.name;
    return bank.address;
    // - should return the banks name and address
  }

  render = function() {
    return checking;
    return savings;
    // should take the current balances of the savings and checking accounts,
    // then update the DOM display of the bank account balances
  }

  makeTransaction = function() {
  // - make a transaction - `makeTransaction`
    function deposit() {

    }

    function withdrawal() {

    }

    checking
    savings

    amount
    	// - should accept three arguments -> a transaction type, an account type, and an amount

    	// - should use the appropriate account functions to perform a transaction (deposit or withdrawal)

    	// - should render the new balances
  }
}
}
